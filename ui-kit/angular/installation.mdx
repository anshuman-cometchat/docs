---
title: Installation
description: "Quick reference for installing CometChat UIKit Angular"
---

## Quick Install

```bash
npm install @cometchat/chat-uikit-angular @cometchat/chat-sdk-javascript
```

Then import the CSS variables in your `angular.json` styles array or global stylesheet:

```css
@import '@cometchat/chat-uikit-angular/src/lib/styles/css-variables.css';
```

<Tip>
If the `@import` doesn't resolve, use the full `node_modules` path instead:

```css
@import 'node_modules/@cometchat/chat-uikit-angular/src/lib/styles/css-variables.css';
```

Or add it directly in your `angular.json` styles array:

```json
"styles": [
  "src/styles.css",
  "node_modules/@cometchat/chat-uikit-angular/src/lib/styles/css-variables.css"
]
```
</Tip>

## Register UIKit Assets

The UIKit ships icons, images, and audio files that Angular's build system won't serve automatically. Add the assets path to your `angular.json` so they are copied to the build output:

```json expandable
{
  "projects": {
    "your-app": {
      "architect": {
        "build": {
          "options": {
            "assets": [
              {
                "glob": "**/*",
                "input": "node_modules/@cometchat/chat-uikit-angular/src/lib/assets",
                "output": "assets"
              }
            ]
          }
        }
      }
    }
  }
}
```

<Warning>
  Skipping this step will cause all UIKit icons, empty-state images, and notification sounds to fail with 404 errors.
</Warning>

<Info>
  For the full setup guide — including initialization, authentication, and your first component — see the [Angular Integration Guide](/ui-kit/angular/angular-integration).
</Info>
